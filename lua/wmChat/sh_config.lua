AddCSLuaFile()

wmChat.config = {}

local CONFIG = wmChat.config

CONFIG.ChatTimeFadeout = 7

CONFIG.ConsoleMessageChat = {
    name = "[CHAT MESSAGE]",
    head = Color(0, 0, 178),
    body = Color(0, 128, 255),
}

CONFIG.ConsoleMessageServer = {
    name = "[SERVER MESSAGE]",
    head = Color(0, 178, 0),
    body = Color(0, 128, 255),
}

CONFIG.ServerMessageColor = Color(0, 128, 255)